3️⃣ PROMPT #0 — INTRODUZIONE & REGOLE ARCHITETTURALI

(questo è il primo che gli darai)

Lo scrivo già pronto da copiare-incollare.

📌 PROMPT #0 – ARCHITETTURA GENERALE & REGOLE

Stiamo lavorando a una web app basata su Laravel + JavaScript modulare (SPA-style).
L’obiettivo è implementare la pagina di Create / Modify Order, che deve essere complessa ma rigorosamente strutturata.

ATTENZIONE: non improvvisare. Segui ESATTAMENTE le regole sotto.

🧱 ARCHITETTURA GENERALE

L’app segue lo stesso pattern già usato per Home e Orders:

*.state.js → stato centrale

*.api.js → chiamate HTTP

*.actions.js → logica applicativa

*.hydration.js → inizializzazione stato

*.render.js → orchestrazione rendering

*.view.js → DOM refs

NON mettere tutta la logica in un unico file

NON creare classi componenti dentro la pagina

🧩 COMPONENTI UI

Tutti i componenti UI:

vanno in resources/js/components

devono essere riutilizzabili

accettano SOLO props

NON gestiscono stato applicativo

NON fanno fetch

Un componente:

riceve dati

renderizza

emette eventi

fine

Se un componente decide “da solo” cosa fare → è sbagliato.

🔁 RIUSO COMPONENTI

Se esiste già un componente (scheduler, top bar, sidebar, ecc):

VA RIUTILIZZATO

NON duplicato

NON riscritto

Prendere riferimento solo da codice esistente funzionante (es. Home)

🌑 TEMA GRAFICO

L’intera app è dark mode

NON usare classi light

NON inventare nuove UI

Seguire fedelmente le reference fornite

Se qualcosa non è chiaro → chiedere

🚫 ERRORI DA NON RIPETERE

❌ Tutto in orders.render.js

❌ Logica dentro i componenti

❌ Dati hardcoded

❌ UI diversa dalle reference

❌ Fetch fatti dai componenti

❌ Stato duplicato

✅ APPROCCIO RICHIESTO

Separazione netta delle responsabilità

Backend = fonte di verità

Frontend = stato derivato

Componenti = rendering puro

Se qualcosa non è chiaro, fermati e fai domande prima di implementare.
Non procedere “a intuito”.